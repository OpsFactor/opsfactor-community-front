/** Keeps existing host imports stable while notifications are Community-owned. */
export { useFrontendNotificationsStore as useNotificationsStore } from '@opsfactor/front-core';
export type {
  FrontendNotification as OfxNotification,
  FrontendNotificationTone as OfxNotificationTone,
} from '@opsfactor/front-core';
