/** Raw enum values published by the Community Material Status catalog endpoint. */
export type CommunityMaterialStatus = string;

/** Single server-owned catalog endpoint; it does not receive material identifiers. */
export const MATERIAL_STATUS_CATALOG_ENDPOINT = '/api/secured/material/status';
