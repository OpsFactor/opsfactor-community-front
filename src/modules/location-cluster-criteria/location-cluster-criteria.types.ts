/** Raw enum value published by the Community Location Cluster criteria catalog. */
export type CommunityLocationClusterCriterion = string;

/** Server-owned allowed-value catalog; it receives no cluster or material identifiers. */
export const LOCATION_CLUSTER_CRITERIA_CATALOG_ENDPOINT = '/api/secured/clustering/location/criteria';
